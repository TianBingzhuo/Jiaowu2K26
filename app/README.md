# jiaowu2K26 正式程序工作区

本目录是正式程序与文档门户的物理边界。项目已进入 `hacking / GATE-1 / P0-00`，允许在这里建立最小技术基线；**当前仍不包含已验收产品功能**。

- 文档门户位于 `docs-site/`。
- 产品、架构、模块与赛事事实目前仍以项目根目录既有 Markdown 和 `PROJECT-MANIFEST.json` 为准。
- 当前只允许建立 `apps/`、`crates/`、`packages/` 与根级 `contracts/` 的最小骨架、环境脚本和一个真实纵向验证；不得提前展开 P0-01 以后的功能。
- 最终结构和技术采用结论以 GATE-1 的实验与用户审核为准；Rust、OceanBase、Tauri、Unity 或 Unreal 都不能仅凭文档变成既定依赖。
